trigger:
- main

pool:
  vmImage: 'ubuntu-latest'

stages:

- stage: Build
  jobs:
  - job: BuildJob
    steps:
    - script: mvn clean package -DskipTests
      displayName: 'Build Project'

- stage: Test
  dependsOn: Build
  jobs:
  - job: TestJob
    steps:
    - script: mvn -B test -DforkCount=0 -DreuseForks=false
      displayName: 'Run Tests Safely'

    - task: PublishTestResults@2
      inputs:
        testResultsFiles: '**/surefire-reports/TEST-*.xml'